export interface RankingList {
  companyIcon: string;
  companyName: string;
  name: string;
  accept: string;
  pending: string;
  cancel: string;
}

export interface TableLogs {
  date: number | string;
  description: string;
  icon: string;
}
