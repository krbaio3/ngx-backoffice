import { CapitalizeSpacePipe } from './capitalize-space.pipe';

describe('CapitalizeSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
